open this directory in your command line
```
erl
c (four). // where four = filename
four:partOne(). // where partOne = function
```

To run debugger:
```
erl
c(four, debug_info).
debugger:start(). // then attach...
```